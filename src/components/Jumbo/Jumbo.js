import React from 'react';
import './Jumbo.css';

const Jumbo = props => (

  <div className="jumbotron">
    <h1 className="display-3 text-center">Ready to play?</h1>
    <h3 className="text-center">Don't be a hero. Click each once, but never more than once...or else.</h3>
  </div>

);

export default Jumbo;
